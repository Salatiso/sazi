const express = require('express');
const router = express.Router();

// Placeholder for class management routes
router.get('/', (req, res) => {
    res.json({ message: 'Class management API' });
});

module.exports = router;
