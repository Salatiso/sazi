const express = require('express');
const router = express.Router();

// Placeholder for lesson management routes
router.get('/', (req, res) => {
    res.json({ message: 'Lesson management API' });
});

module.exports = router;
